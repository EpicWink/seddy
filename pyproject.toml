[build-system]
build-backend = "setuptools.build_meta"
requires = [
    "setuptools >= 42.0, < 84.0",
    "setuptools-scm[toml] >= 10.2.1",
]

[tool.setuptools_scm]

[tool.isort]
length_sort_straight = true
profile = "black"
